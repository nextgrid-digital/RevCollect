export {
  workspaceCanvasPadding as inboxCanvasPadding,
  workspaceCard as inboxContextCard,
  workspaceContextCardSticky as inboxContextCardSticky,
  workspaceContextWidth as inboxContextWidth,
  workspaceCenterMaxWidth as inboxCenterMaxWidth,
  workspaceListCard as inboxListCard,
  workspaceListWidth as inboxListWidth
} from '../../lib/workspace-layout';
