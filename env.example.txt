# =================================================================
# RevCollect Data Layer
# =================================================================
# mock (default) — uses src/features/revcollect/api/mock-service.ts
# supabase — not yet implemented; reserved for backend sprint

NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_REV_COLLECT_DATA_SOURCE=xero

# Data source:
#   mock  — demo customers/invoices/inbox
#   xero  — live customers/invoices/aging from connected Xero (default in app when set)
# NEXT_PUBLIC_REV_COLLECT_DATA_SOURCE=xero

# =================================================================
# Supabase Auth (required for login / signup)
# =================================================================
# Project Settings → API in https://supabase.com/dashboard
# Site URL: http://localhost:3000 (prod: https://app.revcollect.ai)
# Redirect URLs:
#   http://localhost:3000/auth/callback
#   https://app.revcollect.ai/auth/callback
#
# Google login (Authentication → Providers → Google):
#   Enable Google provider and paste Client ID + Client Secret from Google Cloud.
#   In Google Cloud, add Authorized redirect URI:
#   https://YOUR_PROJECT_REF.supabase.co/auth/v1/callback

# NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=  # optional alias for anon/publishable key

# SUPABASE_SECRET_KEY=  # server-only service role; required on Vercel for Xero/Gmail token storage
# Run supabase/migrations/20260722100000_integration_secrets.sql in the Supabase SQL editor.

# =================================================================
# Gmail OAuth (required for real Gmail connect flow)
# =================================================================
# Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/integrations/gmail/callback

# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/integrations/gmail/callback
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# EMAIL_ENCRYPTION_KEY=

# =================================================================
# Xero OAuth (required for real Xero connect flow)
# =================================================================
# Create an app at https://developer.xero.com/app/manage
# Redirect URI: http://localhost:3000/api/integrations/xero/callback
# Scopes: openid profile email offline_access accounting.contacts
#          accounting.invoices accounting.payments accounting.reports.aged.read
# Do not request accounting.transactions — Xero apps created after March 2026 return invalid_scope.

# XERO_CLIENT_ID=
# XERO_CLIENT_SECRET=
# XERO_OAUTH_REDIRECT_URI=http://localhost:3000/api/integrations/xero/callback
# Sales account code used when creating invoices from PDF import (AU often 200, US demo 400)
# XERO_SALES_ACCOUNT_CODE=200

# Optional: Gemini for PDF invoice field extraction
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-flash-latest

# =================================================================
# AI gateway (OpenAI-compatible — not OpenRouter)
# =================================================================
# Used by src/lib/ai/complete.ts and the eve ARI agent.
# If unset, ARI falls back to deterministic template drafts.

# AI_GATEWAY_API_KEY=
# AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1
# AI_BASE_URL=https://api.openai.com/v1
# AI_API_KEY=
# AI_MODEL_GENERATE=gpt-4.1-mini
# AI_MODEL_EXTRACT=gpt-4.1-mini
# AI_MODEL_CLASSIFY=gpt-4.1-mini
# eve's durable ARI runtime expects Node 24+. The Next.js app still runs on Node 22;
# POST /api/ari/run queues drafts without the eve scheduler.

# =================================================================
# Gemini (AR audit narrative summarization)
# =================================================================
# Server-only. Used by POST /api/audit/summarize after deterministic math.
# Get a key: https://aistudio.google.com/apikey

# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-flash-latest

# IMPORTANT: This template supports Clerk's keyless mode!
# You can start using the app immediately without any configuration.
# When you're ready to claim your application, simply click the Clerk
# popup at the bottom of the screen to get your API keys.

# Required: Clerk API Keys (Leave empty for keyless mode)

# Authentication Redirect URLs
# These control where users are directed after authentication actions

# =================================================================
# Clerk Organizations Configuration
# =================================================================
# To enable Organizations in your Clerk Dashboard:
# 1. Go to https://dashboard.clerk.com
# 2. Navigate to Organizations Settings
# 3. Enable "Organizations"
# 4. Configure default roles (admin, member, viewer) if needed
# 5. Set membership limits (default: 5 members on Free plan)
#
# Development Plan Features:
# - Up to 50 Monthly Active Organizations (MAOs) in development
# - Up to 5 members per organization on Free plan
# - Unlimited members on Pro plan
#
# For more information, see: https://clerk.com/docs/organizations/overview

# =================================================================
# Clerk Webhooks (Optional - for production billing)
# =================================================================
# If you're using Clerk Billing or webhooks, add your webhook secret here.
# You can find this in Clerk Dashboard > Webhooks

# =================================================================
# Build Configuration
# =================================================================
# Set to 'true' when deploying with Docker or self-hosting on a VPS.
# This enables Next.js standalone output mode for smaller deployments.

BUILD_STANDALONE=    # Example: "true"

# =================================================================
# Error Tracking Configuration (Sentry)
# =================================================================
# To set up Sentry error tracking:
# 1. Create an account at https://sentry.io
# 2. Create a new project for Next.js
# 3. Follow the setup instructions below

# Step 1: Sentry DSN (Required)
# Found at: Settings > Projects > [Your Project] > Client Keys (DSN)

NEXT_PUBLIC_SENTRY_DSN=    #Example: https://****@****.ingest.sentry.io/****

# Step 2: Organization & Project Details
# Found at: Settings > Organization > General Settings

NEXT_PUBLIC_SENTRY_ORG=  # Example: acme-corp
NEXT_PUBLIC_SENTRY_PROJECT=  # Example: nextjs-dashboard

# Step 3: Sentry Auth Token

# Sentry can automatically provide readable stack traces for errors using source maps, requiring a Sentry auth token.
# More info: https://docs.sentry.io/platforms/javascript/guides/nextjs/manual-setup/#step-4-add-readable-stack-traces-with-source-maps-optional

SENTRY_AUTH_TOKEN=    #Example: sntrys_************************************

# Step 4: Environment Control (Optional)
# Set to 'true' to disable Sentry in development

NEXT_PUBLIC_SENTRY_DISABLED= "false"

# =================================================================
# Important Notes:
# =================================================================
# 1. Rename this file to '.env' for local development
# 2. Never commit the actual '.env' file to version control
# 3. Make sure to replace all placeholder values with real ones
# 4. Keep your secret keys private and never share them
